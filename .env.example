# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# When adding additional env variables, the schema in /env/schema.mjs should be updated accordingly

# Prisma
# MongoDB Connector DATABASE_URL="mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER_URL>/<DB_NAME>?retryWrites=true&w=majority"
# MySQL Connector DATABASE_URL="mysql://<USERNAME>:<PASSWORD>@<HOST>:<PORT>/<DB_NAME>" (Work for MariaDB as well)

 DATABASE_URL="mysql://aa:123@host:port/user_user?retryWrites=true&w=majority"

# Next Auth
# You can generate the secret via 'openssl rand -base64 32' on Linux
# More info: https://next-auth.js.org/configuration/options#secret
# NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000"

# Next Auth Google Provider
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Example Email Env
EMAIL_HOST="smtp.provider.de"
EMAIL_PORT=999
EMAIL_USER="at@email.com"
EMAIL_PASSWORD="yourpassword"
EMAIL_FROM="Name <at@email.com>"